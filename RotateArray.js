/**
 * @param {number[]} nums
 * @param {number} k
 * @return {void} Do not return anything, modify nums in-place instead.
 */

let numbers =[1,2,3,4,5,6,7]
function rotate (nums, k){
   
    // nums.splice(0, nums.length, ... nums.slice(nums.length-k % nums.length), ...nums.slice(0, nums.length-k % nums.length));
    
    
}
rotate(numbers, 3)
console.log(numbers)
